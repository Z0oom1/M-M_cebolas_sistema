# ===========================================
# M&M Cebolas - Modelo .env para PRODUÇÃO (VPS)
# Copie para .env e preencha os valores.
# ===========================================

# Obrigatório: segredo para tokens JWT (gere um forte em produção)
JWT_SECRET=altere_para_uma_chave_longa_e_aleatoria

# Senhas dos usuários iniciais (serão hasheadas no primeiro start)
ADMIN_PASSWORD=senha_admin_forte
VINICIUS_PASSWORD=senha_vinicius
FUNCIONARIO_PASSWORD=senha_funcionario

# NF-e: producao = SEFAZ real; homolgacao ou vazio = simulação
NFE_MODO=producao

# Senha do certificado digital A1 (.pfx) em server/certificado/certificado.pfx
CERT_PASSWORD=senha_do_certificado_pfx

# Porta do Node (Nginx fará proxy 80/443 → 3000)
PORT=3000
